import { css } from 'lit';

export const cardStyles = css`
  .card {
    background: var(--yy-bg-surface);
    border: 1px solid var(--yy-border);
    border-radius: var(--yy-radius-lg);
    box-shadow: var(--yy-shadow);
    padding: var(--yy-space-lg);
  }
`;
